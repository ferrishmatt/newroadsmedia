{% if admin.hasRoute('edit') %}
    <a href="{{ admin.generateObjectUrl('grantResumeAccess', object) }}" class="btn btn-sm btn-default">
        <i class="glyphicon glyphicon-zoom-in"></i>
        Grant Resume Search
    </a>
{% endif %}